- name: Copy file to {{ component }}.service
  ansible.builtin.template:
    src: "{{ component }}.service.j2"
    dest: "/etc/systemd/system/{{ component }}.service"

- name: demon-reload
  ansible.builtin.systemd_service:
    daemon_reload: true
